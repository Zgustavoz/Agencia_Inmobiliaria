// Configuración de MapBox
export const MAPBOX_CONFIG = {
  accessToken: import.meta.env.VITE_MAPBOX_ACCESS_TOKEN,
  defaultCenter: [-73.5577, -2.1883], // Coordenadas por defecto (Ecuador)
  defaultZoom: 5,
  defaultPitch: 0,
  defaultBearing: 0
}
